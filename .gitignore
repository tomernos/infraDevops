# Terraform
.terraform/
# .terraform.lock.hcl is INTENTIONALLY committed (per live unit) — it pins exact provider
# versions + checksums so every dev and CI run resolves identical providers. Do not ignore it.
*.tfstate
*.tfstate.backup
*.tfplan
crash.log
override.tf
override.tf.json

# Secrets
*.pem
*.key
serviceAccountKey.json
.env

# OS
.DS_Store

# Terragrunt cache
.terragrunt-cache/

# Local docs (generated, not tracked)
docs/
terraformlogerror.txt
*.log

terraform-deploy.yaml
terraform-plan-apply.yml
workflowerror.md
# Docs live in ReferencesContext, not in the code repo
CLAUDE.md
NETWORKING.md
